{
  "name": "lurnic-monorepo",
  "private": true,
  "description": "Monorepo root; Next.js app lives in frontend/. Vercel uses vercel.json for install/build.",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
