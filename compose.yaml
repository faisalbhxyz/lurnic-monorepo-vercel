# Optional entrypoint: `docker compose up` resolves compose.yaml before docker-compose.yaml.
# Stack definition lives in docker-compose.yaml (also the file Coolify defaults to).
include:
  - docker-compose.yaml
